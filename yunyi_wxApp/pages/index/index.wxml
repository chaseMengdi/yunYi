<view class="container">
  <!-- 登录注册按钮 -->
  <view class="loginBtnView">
    <button class="loginBtn" type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" open-type="getUserInfo" bindgetuserinfo="login">微信登陆</button>
    <view>无账户自动注册</view>
  </view>

</view>